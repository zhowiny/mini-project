const constant = require('./const');
const request = require('./request');


function doLogin(options, callback) {
   
}

/**
 * 通过token登录
 * @param params
 */
function loginWithToken(params, callback) {
   
}

/**
 * 通过jscode登录
 * @param params
 */
function loginWithJsCode(params, callback) {
   
}


module.exports = {
    doLogin,
};